# ── Credentials — NEVER commit these ──────────────────────
.env
*.env
secrets.json
credentials.json
gmail_credentials.py

# ── Private trading data — NEVER commit these ──────────────
trades.csv
*.csv
positions.json
my_positions.json
accounts.json
gate_state.json
crypto_signal_state.json
global_scores.json
universe_failure_log.json
run_manifest.json

# ── ML model cache — binary, not needed in repo ────────────
*.pkl
*.pickle
ml_model_cache*

# history/ is committed — used by regime_predictor for ML training (~5KB/day)
# history/  ← uncommented intentionally

# ── Local Python ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
venv/
env/
.venv/

# ── Local data files (generated on each run, committed by bot) ──
# These ARE committed by the Actions bot — just not by you manually
# latest_brief.json     ← committed by bot
# dashboard.html        ← committed by bot
# signal_accuracy.json  ← committed by bot

# Local-only output files (not needed on GitHub)
news_analysis.json
content_output.json
brief_history_*.json
*.log

# ── OS junk ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Positions (private — stored in browser, never in repo) ──
positions.json
my_positions.json

# ── Congressional engine cache (ephemeral, refreshed each run) ──
congressional_cache.json
congressional_signals.json

# Don't gitignore strategy_version.json — it's the OOS audit trail

__pycache__/
   *.pyc
   *.pyo
