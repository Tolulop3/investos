{
  "name": "investos",
  "private": true,
  "description": "Netlify Functions dependencies for the InvestOS ticker proxy (netlify/functions/ticker.js).",
  "dependencies": {
    "@netlify/blobs": "^11.0.1"
  }
}
